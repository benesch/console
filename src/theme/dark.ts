/**
 * @module
 * Dark theme colors.
 *
 */

import colors from "./colors";

const semanticColors = {
  bg: colors.gray[900],
  divider: colors.gray[700],
  card: {
    bg: colors.gray[900],
    border: colors.gray[700],
  },
  dropdown: {
    bg: colors.gray[700],
  },
  grayText: colors.gray[200],
};

export default semanticColors;
